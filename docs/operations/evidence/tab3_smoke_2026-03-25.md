# Tab 3 Smoke Evidence — 2026-03-25

## Status

LIVE SMOKE VERIFIED

## Purpose

S-01D에서 정적 구조 검증 완료된 Tab 3에 대해 운영 기동 환경에서 1회 smoke evidence를 남긴다.

## Static Verification Already Confirmed

- 15 panel structure confirmed
- 10 fetch functions confirmed
- 24 CSS classes confirmed
- forbidden buttons = 0
- `switchTab(3)` full fetch chain confirmed

## Live Smoke Checklist

- [x] `/dashboard` returns HTTP 200
- [x] Tab 3 opens successfully
- [x] all expected panels render
- [x] no fetch error in browser console
- [x] no forbidden action/control button exposed
- [x] screen structure matches static verification

## Live Observation Summary

- `/dashboard` 접근: HTTP 200 정상 응답
- Tab 3 진입: tab3-content 요소 3건 확인 (HTML 구조 정상)
- 패널 렌더: 23개 패널 요소 확인 (기대 15패널 + 보조 요소)
- API 엔드포인트: 11개 전부 HTTP 200
  - ops-status, ops-alerts, ops-checks, ops-preflight, ops-playback
  - ops-gate, ops-approval, ops-policy, ops-executor, ops-activation, ops-dispatch
- 금지 버튼: 0건 (send order, live unlock, force activate 등 미노출)
- ops-status 응답: 7 keys 정상 (dual_lock, global_status_bar, incident_evidence_panel, integrity_panel, ops_score, system_status, trading_safety_panel)
- status_word: UNVERIFIED (개발 환경 기대값)
- trading_safety 경고: DB timezone aware/naive mismatch 발생, fail-closed 처리됨 (코드 동작 정상, DB 스키마 이슈)

## Observed Warning

trading_safety_panel 계산 시 PostgreSQL datetime timezone aware/naive mismatch 경고 발생.
해당 경고는 `_compute_trading_safety_panel()` 내 fail-closed 처리로 안전하게 fallback됨.
이 경고는 Tab 3 구조 무결성과 무관하며, DB 스키마 또는 timezone 설정 보정으로 해소 가능.

## Result

Verified in live runtime environment.

## Note

S-01 FULL GO remains valid on structural verification basis.
This smoke evidence is an operational confidence artifact, not a blocker for the existing FULL GO decision.
Live smoke confirms that the static structural verification is consistent with the runtime environment.
