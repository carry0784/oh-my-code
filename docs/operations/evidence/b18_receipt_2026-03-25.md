# B-18 헌법 조항 대조 검수본 — 전략 모델 (Strategy Model)

## 1. 요구 조항

| 조항 | 요구 |
|------|------|
| 목표 | Tab 2 B-17 아래에 Strategy Context 3-섹션 추가 |
| 구성 | Signal Pipeline + Exposure Overview + Execution Readiness |
| 원칙 | badge + count + list only, 설명 문장 금지, AI 추론 금지 |
| 데이터 | data/v2 단일 payload만, 신규 endpoint 없음 |

## 2. 반영 문장

| 변경 | 위치 | 상세 |
|------|------|------|
| HTML | B-17 아래, events-block-t2 위 | `#strategy-context` 접이식 섹션 (3-column grid + recent) |
| JS | `renderTab2()` 내 호출 | `renderStrategyContext(data)` + `_stRow()` helper |
| CSS | dashboard.css append | `.stctx-*` 클래스 45줄 |

## 3. 금지 조항 확인

| 금지 항목 | 위반 여부 |
|----------|----------|
| backend 변경 | **미위반** |
| Tab 1/Tab 3 변경 | **미위반** |
| B-17 섹션 변경 | **미위반** |
| B-19 요소 선반영 | **미위반** |
| AI 추론/추천/생성 | **미위반** |
| blocked_reasons/next_safe_steps 포함 | **미위반** |
| 설명 문장 | **미위반** (badge + count + list only) |
| JS 추가 정렬/점수화 | **미위반** (payload 순서 그대로) |

## 4. 상태 전이표 + 데이터 가용성 표

### 필드 매핑 + Fallback Reason Registry

#### Signal Pipeline

| 필드 | JSON 키 | 현재 값 | UI 표시 | 이유 코드 | 가용성 |
|------|---------|--------|--------|----------|--------|
| 24h 총 신호 | `signal_summary.total_24h` | 0 | **0** | (정상) | **안정** |
| 검증됨 | `signal_summary.validated` | 0 | **0** | (정상) | **안정** |
| 거부됨 | `signal_summary.rejected` | 0 | **0** | (정상) | **안정** |
| 실행됨 | `signal_summary.executed` | 0 | **0** | (정상) | **안정** |
| 대기 | `signal_summary.pending` | 0 | **0** | (정상) | **안정** |
| 거부율 | `ai_assist_sources.signal_pipeline.rejection_rate` | null | **-** | **NULL_FROM_API** | **불안정** (total=0일 때 null) |
| 최근 신호 | `signal_summary.recent` | [] | **최근 신호 없음** | **NO_DATA** | **안정** (빈 배열 정상 반환) |

#### Exposure Overview

| 필드 | JSON 키 | 현재 값 | UI 표시 | 이유 코드 | 가용성 |
|------|---------|--------|--------|----------|--------|
| 총 포지션 | `ai_assist_sources.position_overview.total_positions` | 0 | **0** | (정상) | **안정** |
| 총 평가금액 | `ai_assist_sources.position_overview.total_value` | 0 | **0.00** | (정상) | **안정** |
| 미실현 PNL | `ai_assist_sources.position_overview.unrealized_pnl` | 0 | **0.0000** | (정상) | **안정** |
| 연결 거래소 | `ai_assist_sources.position_overview.exchanges_connected/total` | 6/6 | **6/6** | (정상) | **안정** |
| 미결 주문 | `Σ open_orders[ex].length` | 0 | **0** | (정상) | **안정** |
| 주문 상세 | `open_orders` | 전부 [] | **주문 없음** | **NO_DATA** | **안정** |

#### Execution Readiness

| 필드 | JSON 키 | 현재 값 | UI 표시 | 이유 코드 | 가용성 |
|------|---------|--------|--------|----------|--------|
| Ops Score | `ai_assist_sources.ops_summary.ops_score_average` | 0.68 | **0.68/0.7** | (정상) | **안정** |
| Gate | `ai_assist_sources.ops_summary.gate_decision` | CLOSED | **CLOSED** (red) | (정상) | **안정** |
| Preflight | `ai_assist_sources.ops_summary.preflight_decision` | NOT_READY | **NOT_READY** (amber) | (정상) | **안정** |
| Approval | `ai_assist_sources.ops_summary.approval_decision` | REJECTED | **REJECTED** (red) | (정상) | **안정** |
| Trading Auth | `ai_assist_sources.ops_summary.trading_authorized` | false | **✗** | (정상) | **안정** |
| System Healthy | `ai_assist_sources.ops_summary.system_healthy` | false | **✗** | (정상) | **안정** |
| Status | `ai_assist_sources.ops_summary.status_word` | UNKNOWN | **UNKNOWN** | (정상) | **안정** |

### 전략 모델 데이터 가용성 요약

| 가용성 | 필드 수 | 비고 |
|--------|--------|------|
| **안정** (항상 값 반환) | **19** | signal_summary 5 + exposure 6 + readiness 7 + recent 1 |
| **불안정** (조건부 null) | **1** | rejection_rate (total=0일 때 null) |

> B-19 실행 모델 설계 시: 20개 필드 중 19개가 안정적. rejection_rate만 조건부 null. 실거래 연결 시 signal_summary.recent와 positions가 실제 행으로 채워질 예정.

## 5. 로그/Audit 필드

| 필드 | 값 |
|------|-----|
| evidence_id | B18-RECEIPT-2026-03-25-01 |
| check_type | B18_STRATEGY_CONTEXT |
| run_date | 2026-03-25 |
| auto_repair_performed | false |

## 6. 미해결 리스크

| # | 리스크 | 심각도 |
|---|--------|--------|
| R-1 | rejection_rate 불안정 (total=0 시 null) | LOW |
| R-2 | 실거래 연결 시 recent/positions 행 렌더 미검증 | LOW (B-15와 동일 패턴) |

## 7. 더 좋은 아이디어 반영 여부

| 아이디어 | 반영 |
|---------|------|
| badge + count + list 중심, 설명 문장 금지 | ✓ |
| exchanges_connected 없으면 `-` (대체 계산 금지) | ✓ |
| recent 정렬 금지 (payload 순서 그대로) | ✓ |
| B-18 후 데이터 가용성 표 제출 | ✓ (위 §4에 포함) |

---

**판정**: **PASS**
**테스트**: Dashboard 243 passed (회귀 0)
**완료 시각**: 2026-03-25T23:15:00+09:00
