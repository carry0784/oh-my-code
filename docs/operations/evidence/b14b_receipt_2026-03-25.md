# B-14B 헌법 조항 대조 검수본

## 1. 요구 조항

| 조항 | 요구 |
|------|------|
| 범위 | Preflight item-level 표시, Gate condition 상세화, Approval next-steps UX 확장 |
| 선행 | B-14A DONE |
| 원칙 | read-only, backend 계약 변경 금지, KI-2/B-16 혼입 금지 |
| 수정 파일 | dashboard.html (JS/HTML), dashboard.css (append only) |

## 2. 반영 문장

### Preflight item-level 표시

| 변경 | 상세 |
|------|------|
| HTML | `#pf-items` div 추가 (pf-reasons 위) |
| JS | `fetchOpsPreflight()`에 items 배열 순회 → 8개 item 개별 렌더링 |
| 표시 | ✓ pass (green) / ✗ fail (red) / ? unknown (muted) + name + observed + reason_codes |

검증 (스크린샷):
- ✓ db_connection: assumed_ok
- ? exchange_snapshot: unknown `STALE_SNAPSHOT`
- ✓ open_orders: assumed_accessible
- ✓ position_sync: assumed_synced
- ✗ last_evidence: store_unavailable `MISSING_EVIDENCE`
- ? lock_reason_resolved: no_security_ctx `MISSING_EVIDENCE`
- ✓ governance_active: true
- ? recent_checks_green: no_checks `CHECKS_NOT_GREEN`

### Gate condition 상세화

| 변경 | 상세 |
|------|------|
| JS | `fetchOpsGate()` conditions 렌더링을 inline span → structured div row로 변경 |
| 표시 | icon + name + (rule_ref) + obs: value + req: value |

검증 (스크린샷):
- ✗ preflight_ready (Art43) obs: NOT_READY req: READY
- ✗ ops_score_above_threshold (Art41) obs: 0.8750 req: >=0.7
- ✓ trading_authorized (Art42) obs: true req: true
- ✗ lockdown_inactive (Art7) obs: no_security_ctx req: false

### Approval next-steps

| 변경 | 상세 |
|------|------|
| HTML | `#apr-next-steps` div 추가 (apr-details 아래) |
| JS | `fetchOpsApproval()`에 rejection_reasons → stepMap 고정 매핑 → next-steps 렌더링 |
| 표시 | "Next: Resolve gate conditions \| Check preflight panel \| ..." |

검증 (스크린샷):
- Next: Resolve gate conditions \| Check preflight panel \| Improve ops score above 0.70 \| Resolve lockdown state

### CSS (append only)

`.pf-item`, `.pf-item-pass/fail/unknown`, `.gate-cond`, `.gate-cond-met/unmet`, `.apr-next-steps`, `.apr-guidance` 추가.

## 3. 금지 조항 확인

| 금지 항목 | 위반 여부 |
|----------|----------|
| backend 수정 | **미위반** — dashboard.py, endpoint 코드 미수정 |
| HTML 구조 변경 | **미위반** — 기존 ID 전부 보존, div 2개 append only |
| 기존 fetch 변경 | **최소 변경** — fetchOpsPreflight/Gate/Approval 내부 렌더 확장만 (fetch 경로 무변경) |
| Tab 2/Tab 1 변경 | **미위반** |
| KI-2 혼입 | **미위반** — readiness probe 미수정 |
| B-16 혼입 | **미위반** — 실포지션 row 렌더 확장 미수행 |

## 4. 상태 전이표

| 항목 | 이전 (B-14A) | 이후 (B-14B) |
|------|-------------|-------------|
| Preflight 패널 | decision badge + summary + flat reason_codes | + **8개 item 개별 표시** (icon/name/observed/reasons) |
| Gate 패널 | inline span (icon + name + rule_ref) | **structured row** (icon + name + rule + observed + required) |
| Approval 패널 | rejection reasons 표시만 | + **next-steps 안내** (고정 매핑) |
| 기존 B-14A Safety Summary | 무변경 | 무변경 |
| 테스트 | 300 passed | **300 passed** (회귀 0) |

## 5. 로그/Audit 필드

| 필드 | 값 |
|------|-----|
| evidence_id | B14B-RECEIPT-2026-03-25-01 |
| check_type | B14B_UX_EXTENSION |
| run_date | 2026-03-25 |
| auto_repair_performed | false |

## 6. 미해결 리스크

| # | 리스크 | 심각도 | 비고 |
|---|--------|--------|------|
| R-1 | KI-2 (/ready 503) 미해소 | LOW | 별도 카드 |
| R-2 | 실포지션 존재 시 item-level 변화 미검증 | LOW | B-16 범위 |

## 7. 더 좋은 아이디어 반영 여부

| 아이디어 | 반영 |
|---------|------|
| 카드 우선순위 표준 (같은 축 연장 우선) | 적용 — B-14A→B-14B 연속 수행 |

---

**판정**: **PASS**
**완료 시각**: 2026-03-25T21:50:00+09:00
