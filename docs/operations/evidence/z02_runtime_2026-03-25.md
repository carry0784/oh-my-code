# Z-02 Runtime Verification Report

## 1. 실행 개요

| 필드 | 값 |
|------|-----|
| check_type | Z02_RUNTIME_VERIFY |
| run_date | 2026-03-25 |
| run_timestamp | 2026-03-25T15:05:00+09:00 |
| system_phase | prod (frozen, live) |
| executor_activated | false |
| evidence_id | Z02-2026-03-25-RUNTIME-01 |
| 목적 | Z-02 BLOCK 항목 런타임 검증 |

## 2. 검증 대상

Z-02 Level 1 Daily Check에서 BLOCK으로 판정된 항목의 런타임 직접 확인:

| 원래 항목 | Z-02 판정 | 사유 코드 |
|-----------|----------|-----------|
| #3 /health → 200 | BLOCK | BLOCK_RUNTIME_UNVERIFIED |
| #4 /status → 200, no CRITICAL | BLOCK | BLOCK_RUNTIME_UNVERIFIED |
| #5 /dashboard 로드 | BLOCK | BLOCK_RUNTIME_UNVERIFIED |
| #8 no crash/restart loop | BLOCK | BLOCK_NO_PROCESS_EVIDENCE |

## 3. 결과 표

| # | 항목 | expected | observed | result | evidence_type | confidence | notes |
|---|------|----------|----------|--------|---------------|------------|-------|
| 1 | 서버 기동 상태 | process listening on :8000 | PID=17364; TCP 127.0.0.1:8000 LISTENING | **PASS** | runtime | high | server_running=true; port=8000; pid=17364 |
| 2 | /health → 200 | HTTP 200 | HTTP 200; body={"status":"healthy"} | **PASS** | runtime | high | status_code=200; body_valid=true |
| 3 | /status → 200, no CRITICAL | HTTP 200, no CRITICAL in response | HTTP 404; body={"detail":"Not Found"} | **FAIL** | runtime | high | status_code=404; route_not_registered=true; running_server_version_mismatch=true; /ready=404; /startup=404 동일 증상 |
| 4 | /dashboard 로드 | HTTP 200, HTML 정상 렌더 | HTTP 200; HTML title="K-Dexter Operations Board"; governance banner present; template structure intact | **PASS** | runtime | high | status_code=200; html_rendered=true; title_correct=true; governance_banner=present |
| 5 | crash/restart loop 없음 | no abnormal restart pattern | server stable (PID 17364 active); logs show orderly startup/shutdown cycles; no crash stack traces; latest start=2026-03-25T05:44:32Z; no rapid restart pattern | **PASS** | runtime | high | crash_trace=none; restart_loop=false; orderly_cycles=true; server_uptime_stable=true |

## 4. 전체 판정

| 필드 | 값 |
|------|-----|
| overall_result | **FAIL** |
| operational_continuity | **UNVERIFIED** |
| 판정 근거 | /status 엔드포인트 HTTP 404 (FAIL 1건) |
| 매핑 규칙 | FAIL 존재 → UNVERIFIED |

### 판정 상세

**PASS 항목 (4/5)**:
- 서버 기동 확인됨 (PID 17364, port 8000)
- `/health` → 200, `{"status":"healthy"}`
- `/dashboard` → 200, HTML 정상 렌더 (K-Dexter Operations Board)
- crash/restart loop 없음 (서버 안정, 로그 정상)

**FAIL 항목 (1/5)**:
- `/status` → **404 Not Found**

### FAIL 원인 분석

현재 기동 중인 서버는 현행 코드베이스와 **버전 불일치** 상태:

| 증거 | 관찰 |
|------|------|
| `/health` (main.py:161) | 200 ✓ (등록됨) |
| `/ready` (main.py:168) | 404 ✗ (미등록) |
| `/startup` (main.py:209) | 404 ✗ (미등록) |
| `/status` (main.py:251) | 404 ✗ (미등록) |
| `/dashboard` (main.py:153) | 200 ✓ (등록됨) |
| `/dashboard/api/data/v2` (dashboard.py:87) | 500 (등록됨, DB 연결 실패) |
| `/dashboard/api/ops-status` (dashboard.py:1718) | 404 ✗ (미등록) |

**해석**: 서버가 C-03(readiness/startup probes), C-04(degraded status), 후기 ops-* 라우트가 추가되기 이전의 코드 버전으로 기동되어 있음. `/health`과 기본 dashboard는 정상이나, 이후 추가된 엔드포인트들은 런타임에 등록되지 않음.

### 보조 관찰 (검증 범위 외, 기록용)

| 관찰 | 상세 |
|------|------|
| `/dashboard/api/data/v2` → 500 | DB 연결 실패 (PostgreSQL async 관련) |
| `ops_trading_safety_failed` 경고 | timezone aware/naive mismatch (BL-TZ01 건) |
| `ops_trading_safety_failed` 경고 | enum orderstatus REJECTED 미등록 (BL-ENUM01 건) |

## 5. 후속 조치

| 필드 | 값 |
|------|-----|
| operator_action_required | **manual_investigation_required** |
| auto_repair_performed | **false** |

### 필요 조치

1. **서버 재기동 (현행 코드 반영)** — 현재 서버를 중지하고 현행 코드베이스로 `uvicorn app.main:app --reload --port 8000` 재기동
2. **재기동 후 /status 200 확인** — `/status`, `/ready`, `/startup` 응답 검증
3. **재기동 후 Z-02 런타임 재검증** — 5개 항목 재확인
4. **DB 연결 확인** — `/dashboard/api/data/v2` 500 응답 원인 조사 (PostgreSQL 기동 상태, 마이그레이션 상태)

### 주의 사항
- 서버 재기동은 **운영자 판단**으로 수행 (자동 복구 금지)
- 코드 수정 없이 재기동만 수행
- 재기동 후 반드시 검증 수행

## 6. Evidence 경로

| 항목 | 경로 |
|------|------|
| 본 런타임 검증 | `docs/operations/evidence/z02_runtime_2026-03-25.md` |
| Z-02 일일 점검본 | `docs/operations/evidence/z02_daily_2026-03-25.md` |
| 프로토콜 정의 | `docs/z02_operational_check_protocol.md` |

---

**검증 완료 시각**: 2026-03-25T15:05:00+09:00
**검증 수행자**: Claude Code (automated, read-only)
**자동 복구 수행**: false
**다음 단계**: 운영자 판단에 따른 서버 재기동 → 런타임 재검증
