# B-16 헌법 조항 대조 검수본

## 1. 요구 조항

| 조항 | 요구 |
|------|------|
| 목표 | Tab 1 하단에 전체 거래소 총계 요약 바 추가 |
| 필드 | 거래소 수 / 연결됨 / 총 포지션 / 총 매매 / 총 평가금액 / 총 미실현PNL |
| 집계 | data/v2 기준 venue만, stats 제외, okx 패널 없음 |
| 금지 | backend, Tab 2/3, KI-2, 기존 ID 변경 |

## 2. 반영 문장

| 변경 | 상세 | 위치 |
|------|------|------|
| HTML | `#exchange-summary-bar` div 추가 (6 필드: esb-venue-count, esb-connected, esb-positions, esb-trades, esb-total-value, esb-pnl) | compare-grid 아래, freshness-block 위 |
| JS | `renderExchangeSummaryBar(data, venueStates)` 함수 추가 → `renderAll()` 내에서 호출 | renderVenueCards 직후 |
| CSS | `.exchange-summary-bar`, `.esb-item`, `.esb-label`, `.esb-value` append only | dashboard.css 하단 |

## 3. 금지 조항 확인

| 금지 항목 | 위반 여부 |
|----------|----------|
| backend 변경 | **미위반** |
| data/v2 응답 변경 | **미위반** |
| Tab 2/Tab 3 변경 | **미위반** |
| KI-2 혼입 | **미위반** |
| 기존 ID 변경 | **미위반** |

## 4. 상태 전이표 + Fallback Reason Registry

| 필드 | JSON 키 | JSON 값 | UI 표시 | 이유 코드 |
|------|---------|---------|--------|----------|
| 거래소 수 | (카드 수 계산) | 5 | **5** | (정상) |
| 연결됨 | `data[ex].status` | 5/5 connected | **5** | (정상) |
| 총 포지션 | `Σ data[ex].position_count` | 0+0+0+0+0 | **0** | (정상 — 값 존재) |
| 총 매매 | `Σ data[ex].trade_count` | 0+0+0+0+0 | **0** | (정상 — 값 존재) |
| 총 평가금액 | `Σ data[ex].total_value` | null×5 | **-** | **NULL_FROM_API** |
| 총 미실현PNL | `Σ data[ex].unrealized_pnl` | null×5 | **-** | **NULL_FROM_API** |

## 5. 로그/Audit 필드

| 필드 | 값 |
|------|-----|
| evidence_id | B16-RECEIPT-2026-03-25-01 |
| check_type | B16_EXCHANGE_SUMMARY_BAR |
| run_date | 2026-03-25 |
| auto_repair_performed | false |

## 6. 미해결 리스크

| # | 리스크 | 심각도 |
|---|--------|--------|
| R-1 | 총 평가금액/PNL이 `-` (전 거래소 null) | LOW — 실거래 연결 시 자동 표시 |

## 7. 더 좋은 아이디어 반영 여부

| 아이디어 | 반영 |
|---------|------|
| 화면에는 숫자/- 만, 검수본에는 이유 코드 | ✓ — reason chip 미삽입, 검수본에만 이유 코드 |

---

**판정**: **PASS**
**테스트**: Dashboard 243 passed (회귀 0)
**완료 시각**: 2026-03-25T22:30:00+09:00
