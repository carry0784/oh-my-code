# B-20 헌법 조항 대조 검수본 — Timeline / History (통합 타임라인)

## 1. 요구 조항

| 조항 | 요구 |
|------|------|
| 목표 | Tab 2 B-19 아래에 4소스 통합 타임라인 추가 |
| 카테고리 | EVENT / SIGNAL / FRESHNESS / VIOLATION |
| 필터 | JS class toggle only (backend 없음) |
| 정렬 | ISO 8601 시간순 내림차순, 파싱 불가 → 하단 |

## 2. 반영 문장

| 변경 | 위치 | 상세 |
|------|------|------|
| HTML | B-19 아래, events-block-t2 위 | `#unified-timeline` 접이식 섹션 (필터 바 + 리스트) |
| JS | `renderTab2()` 내 호출 | `renderUnifiedTimeline(data)` + `_utlToggle` + `_utlParseTs` + `_utlFormatTs` |
| CSS | dashboard.css append | `.utl-*` + Observation Layer 공통 badge 색 38줄 |

## 3. 금지 조항 확인

| 금지 항목 | 위반 여부 |
|----------|----------|
| backend 변경 | **미위반** |
| Tab 1/Tab 3 변경 | **미위반** |
| B-17/B-18/B-19 변경 | **미위반** |
| 기존 events-list-t2 변경 | **미위반** (별도 블록 유지) |
| AI 추론/추천 | **미위반** |
| 이벤트 생성/삭제 | **미위반** |

## 4. 상태 전이표

### 카테고리별 엔트리

| 카테고리 | 소스 | 현재 건수 | 표시 | 이유 코드 |
|---------|------|----------|------|----------|
| EVENT | `recent_events` | 0 | (미표시) | NO_DATA |
| SIGNAL | `signal_summary.recent` | 0 | (미표시) | NO_DATA |
| FRESHNESS | `source_freshness.sources` (≠fresh) | 15 | venue/quote/runtime unknown | NULL_FROM_API (last_updated=null) |
| VIOLATION | `doctrine.recent_violations` | 0 | (미표시) | NO_DATA |

### 시간 표시

| 조건 | 표시 |
|------|------|
| ISO 8601 파싱 성공 | YYYY-MM-DD HH:mm (로컬) |
| null / 파싱 불가 | `시간 없음` |

## 5. 로그/Audit 필드

| 필드 | 값 |
|------|-----|
| evidence_id | B20-RECEIPT-2026-03-25-01 |
| check_type | B20_UNIFIED_TIMELINE |
| run_date | 2026-03-25 |
| auto_repair_performed | false |

## 6. 미해결 리스크

| # | 리스크 | 심각도 |
|---|--------|--------|
| R-1 | FRESHNESS 엔트리 전부 `시간 없음` (last_updated=null) | LOW — 실거래 연결 시 시간 채워짐 |
| R-2 | EVENT/SIGNAL/VIOLATION 0건 (데이터 없음) | LOW — 실거래 시 자동 채워짐 |

## 7. 더 좋은 아이디어 반영 여부

| 아이디어 | 반영 |
|---------|------|
| Observation Layer 공통 badge 색 고정 | ✓ (EVENT=neutral, SIGNAL=blue, FRESHNESS=amber, VIOLATION=red) |
| badge 텍스트 카테고리명 고정 대문자 | ✓ (EVENT/SIGNAL/FRESHNESS/VIOLATION) |
| 구조 명칭: Operational Context Stack + Observation Layer | ✓ |

---

**판정**: **PASS**
**테스트**: Dashboard 243 passed (회귀 0)
**완료 시각**: 2026-03-25T23:45:00+09:00
