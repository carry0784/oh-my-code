# Hourly Check

## S-01 Hourly Observability Checks

- [ ] `stale_data` is observable
- [ ] `snapshot_age` is observable
- [ ] `recent_warnings` is observable
- [ ] hourly enrichment from `ops-status.integrity_panel` is active
- [ ] unknown-only WARN fallback is not dominating the hourly result
